spring:
  application:
    name: message-broker
  http:
      encoding:
        charset: UTF-8
        force: true
        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7001/eureka
server:
  port: 7003

